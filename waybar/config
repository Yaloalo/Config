{
  "layer": "top",

  // ── center: Hyprland workspaces ────────────────────────────────────────
  "modules-center": ["hyprland/workspaces"],

  // ── right: network, clock, battery ────────────────────────────────────
  "modules-right": [
    "network",
"pulseaudio",
"clock",
    "battery"
  ],

  // ── network status ──────────────────────────────────────────────────────
  "network": {
    "tooltip": false,
    "format-wifi": " {essid}",
    "format-ethernet": ""
  },

// --- volume -------------------------------------------------


  "pulseaudio": {
    "format":       " {volume}%",      // speaker icon + percentage
    "format-muted": " mute",           // alternative when muted
    "tooltip":      false,
    "volume-icon-declick": true         // if you want click‐to‐mute/unmute behavior
  },

  // ── clock: 24h format, date ─────────────────────────────────────────────
  "clock": {
    "format": "{:%H:%M %d/%m/%Y}",
    "format-alt": "{:%A, %d %B %Y  %H:%M:%S}",
    "tooltip-format": "{:%c}"
  },

  // ── battery state ───────────────────────────────────────────────────────
  "battery": {
    "states": {
      "good": 95,
      "warning": 30,
      "critical": 20
    },
    "format": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-plugged": " {capacity}%",
    "format-alt": "{time} {icon}",
    "format-icons": ["","","","",""]
  },

"hyprland/workspaces": {
    // …keep your existing fields…
    "persistent-workspaces": { "*": 5 },
    "move-to-monitor": true,

    // show the scratchpad (special) only when it contains windows
    "show-special": true,
    "special-visible-only": true,

    // render each workspace as its icon
    "format": "{icon}",

    // map IDs and states to icons
"format-icons": {
    "1": "",   // Terminal
    "2": "",   // Settings (gear)
    "3": "",   // Chrome
    "4": "",   // Spotify
    "5": "",   // Chat bubble

    "empty":   "",  // unused workspace
    "visible": "",  // shown on another monitor
"special": "\uE6AE"   // scratchpad (now using the Neovim glyph)
  }
}
}
