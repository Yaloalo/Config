{
  "layer": "top",

  // ── center: Hyprland workspaces ────────────────────────────────────────
  "modules-center": ["hyprland/workspaces"],

  // ── right: network, clock, battery ────────────────────────────────────
  "modules-right": [
    "network",
"pulseaudio",
"clock",
    "battery"
  ],

  // ── network status ──────────────────────────────────────────────────────
  "network": {
    "tooltip": false,
    "format-wifi": " {essid}",
    "format-ethernet": ""
  },

// --- volume -------------------------------------------------


  "pulseaudio": {
    "format":       " {volume}%",      // speaker icon + percentage
    "format-muted": " mute",           // alternative when muted
    "tooltip":      false,
    "volume-icon-declick": true         // if you want click‐to‐mute/unmute behavior
  },

  // ── clock: 24h format, date ─────────────────────────────────────────────
  "clock": {
    "format": "{:%H:%M %d/%m/%Y}",
    "format-alt": "{:%A, %d %B %Y  %H:%M:%S}",
    "tooltip-format": "{:%c}"
  },

  // ── battery state ───────────────────────────────────────────────────────
  "battery": {
    "states": {
      "good": 95,
      "warning": 30,
      "critical": 20
    },
    "format": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-plugged": " {capacity}%",
    "format-alt": "{time} {icon}",
    "format-icons": ["","","","",""]
  },

"hyprland/workspaces": {
  "all-outputs": true,
  "persistent-workspaces": {
    "*": [1, 2, 3, 4, 5]
  },
  "move-to-monitor": true,
  "show-special": true,
  "special-visible-only": true,
  "format": "{icon}",
  "format-icons": {
    "1": "\uEBCA",   // Terminal
    "2": "",        // Settings (gear)
    "3": "\uE745",   // Chrome
    "4": "",        // Spotify
    "5": "",        // Chat bubble
    "empty": "",    // Unused workspace
    "visible": "",  // Workspace visible on other monitor
    "special": "\uE6AE" // Scratchpad icon
  }
}
}
