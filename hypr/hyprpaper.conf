# ~/.config/hypr/hyprpaper.conf

# Enable IPC so you can switch wallpapers at runtime
ipc = true

# ── Preload bundled wallpaper ────────────────────────────────────────────────
preload = /home/yaloalo/.config/wallpapers/default.png

# ── Assign one to each monitor ────────────────────────────────────────────────
# Replace “fill” with center, fit, stretch, tile, etc. as desired.
# Explicit target for your current output (from hyprpaper debug).
wallpaper = HDMI-A-1,/home/yaloalo/.config/wallpapers/default.png
